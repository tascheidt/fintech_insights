.env
*.pyc
__pycache__/
data/*.db
logs/*.log
.venv/
venv/
*.egg-info/
dist/
build/
.DS_Store

# Claude Code: agent definitions (.claude/agents/) and agent memory
# (.claude/agent-memory/) are committed — shared institutional knowledge for
# this single-developer project. Only per-machine local settings and harness
# worktrees are ignored. All Claude config lives at the repo root; nothing
# under web/.claude/.
.claude/worktrees/
.claude/settings.local.json

# Web app (Next.js)
web/node_modules/
web/.next/
web/.env.local
web/.env*.local
web/out/

.cursor/

/temp

# Playwright stray artifacts if the runner is invoked from the repo root
/test-results/