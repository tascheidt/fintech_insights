[
  {
    "version": "2.2.1",
    "date": "2026-05-23",
    "changes": [
      { "type": "fix", "description": "Gemini cost telemetry now bills reasoning (\"thinking\") tokens at the output rate. Gemini 2.5 (both Pro and Flash) returns thinking tokens in `totalTokenCount` without naming them; our formula previously dropped them entirely, undercounting Flash extraction spend by ~3x. Backfilled all 2,936 historical events; recovered $10.57 of previously-hidden cost." }
    ]
  },
  {
    "version": "2.2.0",
    "date": "2026-05-22",
    "changes": [
      { "type": "feature", "description": "Phase 3 — scaled the incumbent lens from RBC alone to all 5 Big-6 Canadian banks. BMO and National Bank join via Phenom CareerConnect (mirror of the RBC scraper); TD and CIBC join via dedicated Workday scrapers built on a shared `workday-utils.ts` (no base class — fork-per-tenant). Each bank seeded `is_active=false`; flip after smoke." },
      { "type": "feature", "description": "Simplii Financial classifier — `workday-cibc.ts` exports `isSimpliiPosting` and runs in log-only mode for now. Once production logs confirm Simplii postings actually surface inside CIBC's Workday tenant, a follow-up adds the `simplii` companies row (tier=fintech) and routes via `companySlugOverride`." },
      { "type": "feature", "description": "Cost-gate calibration script (`web/scripts/calibrate-incumbent-gate.ts`) — read-only Markdown + JSON report on the incumbent corpus: seniority distribution, function-category distribution, pass rate under the production whitelist, simulated pass rates under candidate whitelists (e.g. include `senior`, drop `aml-financial-crime`), and the top-20 near-miss `senior`-in-whitelisted-function titles. Run after each Phase-3 ramp." },
      { "type": "improvement", "description": "Scraper-break canary — the scraper-health alert email now fires on partial-corpus drops, not only outages. For each tracked incumbent bank, if today's new-jobs count is below 50% of the 7-day rolling average AND that rolling average is above 5, a \"Scraper-break canary\" section appears in the next alert email." },
      { "type": "improvement", "description": "Two partial indexes on the read-hot tables (`idx_job_postings_company_active_first_seen`, `idx_companies_tier_active`) for the per-company windowed queries and the cross-company tier joins. Pre-empts the query-plan degradation Luke's plan review flagged at 5× scale." },
      { "type": "improvement", "description": "`/jobs` hard-capped at the 500 most-recent postings (`JOBS_LIST_MAX_ROWS`). Without it, a `?tier=incumbent` or `?tier=all` view would ship ~22 MB of JSON on every page render once the bank ramp finishes." },
      { "type": "improvement", "description": "`decodeHtmlEntities` consolidated into `web/lib/scrapers/utils.ts` (was duplicated twice; scotiabank's copy decoded `&amp;` first — a subtle bug that collapsed doubly-encoded sequences like `&amp;lt;` to `<` instead of leaving them as `&lt;`)." }
    ]
  },
  {
    "version": "2.1.0",
    "date": "2026-05-21",
    "changes": [
      { "type": "feature", "description": "Incumbent intelligence — the Big-6 Canadian banks (starting with RBC) are now tracked as a curated signal lens. Their hiring never enters a dashboard metric, competitive-matrix row, or digest theme; instead it surfaces only in dedicated, clearly-labelled places so bank volume never overwhelms the fintech view." },
      { "type": "feature", "description": "Incumbent Bets dashboard rail — a full-width module surfacing senior Big-6-bank hires (staff+ in AI/ML, Data and Risk-AI), grouped by bank, over the last 30 days. Reads structured fields only, carries an \"excluded from all dashboard metrics\" note, and is omitted entirely on weeks with no qualifying senior hire." },
      { "type": "feature", "description": "Incumbent Watch — the weekly digest now closes its cross-company section with a clearly-bounded interlude on senior big-bank hiring. Each Big-6 bank gets one bullet (\"RBC · 3 senior AI/ML & Data roles\") with a one-line AI interpretation; the block is omitted on weeks with no qualifying senior bank hire, and bank volume stays excluded from the digest totals." },
      { "type": "feature", "description": "Incumbents companies lens — a new tab on the Companies list shows the tracked Big-6 banks as a deliberate, separate view. Incumbent rows carry an \"Incumbent\" badge and a static \"Reference\" marker instead of the fintech pivot signals, and their job counts never mix into the fintech list's sort." },
      { "type": "feature", "description": "Jobs list Tier filter — a Fintech / Incumbent / All control lets you opt into seeing big-bank postings; the list defaults to fintech-only and incumbent jobs carry an \"Incumbent\" badge." },
      { "type": "improvement", "description": "Incumbent company pages — a bank's detail page now reads honestly: an \"Incumbent\" badge, a context note explaining why its counts don't appear on the dashboard, and a \"Senior hiring signal\" panel pairing total open postings with the senior-role signal count." }
    ]
  },
  {
    "version": "2.0.2",
    "date": "2026-05-18",
    "changes": [
      { "type": "fix", "description": "Closed-job detection: jobs that drop out of an ATS feed are once again marked closed. A regression in the Phase 2 description-hash gate left the closure loop passing an object where a job id was expected, so no posting had been marked closed since 2026-04-19 — the \"Closed\" series on the Net hiring flow chart and the \"closed\" half of 7-Day Net both flatlined at zero. Backfilled the 147 missed closures with closed_date set to each job's last_seen_date, so they land on the timeline when they actually happened rather than spiking on the fix date." }
    ]
  },
  {
    "version": "2.0.1",
    "date": "2026-04-28",
    "changes": [
      { "type": "fix", "description": "Mobile rendering: every surface now fits the iPhone-13 viewport with zero horizontal overflow. Companies index toolbar (lens + view toggle) wraps to two rows on mobile and each control scroll-clips inside its own container; rows reflow as stacked cards instead of an unreadable 5-column grid." },
      { "type": "fix", "description": "Dashboard Competitive Matrix and other multi-column rows no longer push the page wider than the viewport — the grid containers now apply min-w-0 so each card's overflow-x-auto can actually clip." },
      { "type": "fix", "description": "Company drill-down header stacks its action buttons (Watch / Careers site / Generate Insight) under the company name on mobile instead of fighting the headline for horizontal space; the headline itself drops from 32px to 26px on small screens." },
      { "type": "improvement", "description": "Mobile-audit Playwright spec ships under MOBILE_AUDIT=1 — captures full-page screenshots and overflow stats per route so future regressions on small screens get caught quickly. Mobile-nav regression spec asserts the hamburger menu opens and routes." }
    ]
  },
  {
    "version": "2.0.0",
    "date": "2026-04-28",
    "changes": [
      { "type": "feature", "description": "Talent Brief v2 — substantial upgrade pairing the SoCal v1.15 visual rebuild with a working editorial pipeline. Every active company now has a working thesis, interpretation, last-change, and 3–6 strategic bets seeded against live hiring data, refreshable on demand or weekly." },
      { "type": "feature", "description": "LLM editorial engine: Pro (ungrounded) call that generates each company's working thesis, interpretation paragraph, last meaningful change, and 3–6 strategic bets with evidence + forward signal. Reuses the most recent company_insights row as background context to avoid stacking a second grounded call. Refreshable via /api/admin/companies/[id]/regenerate-editorial, the regenerate-editorial.ts script, or a weekly GitHub Actions cron." },
      { "type": "feature", "description": "Cross-company theme labels on the Jobs right rail are now author-quality (\"Retail credit card acquisition\", \"Credit and AML compliance\", \"Treasury and regulatory reporting\") rather than mechanical group keys. Cached in cross_company_themes; auto-refreshed when the keyword bag changes; rows link to /jobs?function=… for one-click drill-in." },
      { "type": "feature", "description": "Company drill-down has working \"By bet\" / \"By function\" tabs. The function tab re-shards bets under their function group, with \"see all roles in {group}\" links opening a function-scoped Jobs drawer." },
      { "type": "feature", "description": "Bet auto-suggestions on the editorial form: a \"Suggest from hiring data\" button proposes 1–4 candidate bets derived from the rule-based pivot classifier (acceleration, cold start, going quiet, keyword burst). Editors review, edit, and accept; the candidates are starting points, not finished editorial." },
      { "type": "feature", "description": "Save view + Export CSV on the Jobs page. Save view copies the current filter URL to the clipboard; Export CSV downloads the visible rows as a dated file (count is shown on the button)." },
      { "type": "improvement", "description": "Playwright e2e on every PR: the public visual smoke runs without secrets, and an authed visual smoke runs against real Supabase when the secrets are configured (skipped cleanly on forks). Reports upload as artifacts on every run. New regression spec guards the cross-company themes block." },
      { "type": "improvement", "description": "Editorial pipeline is fully telemetered: every Gemini call writes to gemini_usage_events with a stable callSite + prompt version, and the Pro grounded research from company-insights is reused as context to avoid double-grounding." },
      { "type": "fix", "description": "Jobs page no longer gets stuck in an infinite render loop when filters change. The URL-sync effect now reads window.location.search at call time instead of depending on the live useSearchParams() reference, breaking the router.replace → searchParams → effect → router.replace cycle." },
      { "type": "fix", "description": "Login / marketing chrome shows the canonical 28px BrandMark instead of a generic Sparkles placeholder." }
    ]
  },
  {
    "version": "1.15.0",
    "date": "2026-04-25",
    "changes": [
      { "type": "feature", "description": "Companies index rebuilt as a signal-rich editorial list. Each row carries a working thesis, hiring sparkline, pivot signals (new pattern / accelerating / going quiet / continuity), and the most recent meaningful change." },
      { "type": "feature", "description": "Company drill-down rebuilt bets-first. Working thesis at the top, strategic bets ranked by hiring evidence, each bet bundling claim, evidence (internal hiring + external news), confidence, associated jobs, and a forward signal." },
      { "type": "feature", "description": "Jobs page rebuilt as a list-first browsing surface — sortable table with company / function / location / posted columns, recency filters, plus a contextual right rail showing function heat (30-day net) and cross-company themes." },
      { "type": "feature", "description": "New scoped Jobs drawer on the company drill-down lets you see jobs filtered to a specific bet without leaving the page, with a one-click 'expand to all companies' cross-link." },
      { "type": "improvement", "description": "Marketing landing page is now the public root URL; signed-in users land on /dashboard. Old /welcome bookmarks redirect cleanly." },
      { "type": "improvement", "description": "Admin and labs surfaces fully migrated to design-system tokens — no more raw Tailwind palette utilities. Email templates now share a centralized SoCal color map (web/lib/email/colors.ts)." },
      { "type": "improvement", "description": "Editor-only fields on the company edit form: working thesis, sub-line, interpretation, last-change, and a strategic-bets repeater (with evidence sub-rows). Companies missing a thesis are surfaced in admin under 'Needs editorial'." }
    ]
  },
  {
    "version": "1.14.0",
    "date": "2026-04-25",
    "changes": [
      { "type": "improvement", "description": "Design system aligned with the canonical Talent Brief tokens. Pacific brand blue is now the deep ocean hue (oklch 0.575/0.150/236), Sun is the accent family for CTAs, and Sunset is the highlight family for change markers and alerts." },
      { "type": "improvement", "description": "New brand mark and favicon: gradient sun + coast wave on the warm sand background. Replaces the placeholder check-square mark." },
      { "type": "improvement", "description": "Net Hiring Flow chart rebuilt as bars (Pacific for new, Sun for closed) with a thin foreground net line — replaces the bright red/green soft area fills." },
      { "type": "improvement", "description": "Jobs table redesigned: company avatars, function chips with hot/regular variants, location truncated to City + Country, mono first-seen with `2d ago` formatting, accent-soft active status chip. Whole row is the link target — no more blue-underlined titles." },
      { "type": "improvement", "description": "Audited and replaced ad-hoc Tailwind palette utilities (`text-green-*`, `bg-red-*`, etc.) with design tokens across digests, releases, feedback, settings, jobs, companies, and dashboard surfaces." }
    ]
  },
  {
    "version": "1.13.0",
    "date": "2026-04-25",
    "changes": [
      { "type": "feature", "description": "New SoCal design system: warm sand surfaces, Pacific brand blue, Sunset/Sun accents, Fraunces editorial display font, and a token-driven color/type pipeline. Every visual value now references a single source of truth in `app/globals.css` so theme tweaks land in one line." },
      { "type": "feature", "description": "Editorial moments — weekly digest banner, marketing welcome page, login hero, and company stated-strategy callout — now use the coast gradient and Fraunces display type for a clearer reader experience." },
      { "type": "feature", "description": "New `/digests/archive` route lists every published digest with date, lede, signal density, and year filter." },
      { "type": "feature", "description": "Settings reorganized into Notifications · Sources · Account · Team tabs, with an ATS source-health table." },
      { "type": "feature", "description": "New marketing welcome surface at `/welcome` — gradient hero, live stats, sample digest pull-quote, coverage list." },
      { "type": "improvement", "description": "Job detail page redesigned with a two-column editorial layout: posting body + signals sidebar." },
      { "type": "improvement", "description": "Reusable `EmptyState` component now powers companies, digests, and settings empty states; new shared `SignalsTimeline` component for vertical/responsive signal lists." },
      { "type": "improvement", "description": "ESLint rule `design-system/no-raw-color` warns on raw hex/rgb/oklch literals outside `globals.css` to prevent palette drift." }
    ]
  },
  {
    "version": "1.12.1",
    "date": "2026-04-21",
    "changes": [
      { "type": "fix", "description": "Job structure extraction no longer truncates responses mid-JSON on long descriptions — raised the output token cap to match the runtime config, eliminating the wasted first-call retries that were firing on ~3 EQ Bank jobs per collection run." }
    ]
  },
  {
    "version": "1.12.0",
    "date": "2026-04-19",
    "changes": [
      { "type": "improvement", "description": "Every production Gemini call now logs token usage, model served, grounding flag, latency, and a USD cost estimate to the new `gemini_usage_events` table — enabling per-feature spend visibility and drift detection without waiting for the Google Cloud billing dashboard." }
    ]
  },
  {
    "version": "1.11.0",
    "date": "2026-04-19",
    "changes": [
      { "type": "improvement", "description": "Advanced strategic analysis no longer pre-fetches a duplicate grounded web search call — the main analysis already grounds in-flight, cutting per-job Gemini spend roughly in half without regressing the hard signals (is_new_direction, is_executive_movement, confidence, novelty_score)." }
    ]
  },
  {
    "version": "1.10.0",
    "date": "2026-04-19",
    "changes": [
      { "type": "improvement", "description": "Job ingestion now skips AI re-extraction when a scraped description matches the stored SHA-1 hash, eliminating the bulk of unchanged-description Gemini calls from the daily collection cron." }
    ]
  },
  {
    "version": "1.9.0",
    "date": "2026-04-19",
    "changes": [
      { "type": "improvement", "description": "Switched all Gemini calls from the pinned `gemini-3-flash-preview` preview ID to the floating `gemini-flash-latest` alias, so model upgrades now roll through without code changes." },
      { "type": "improvement", "description": "Refreshed CLAUDE.md `AI Model Requirements` to require `-latest` aliases across Flash, Flash-Lite, and Pro tiers, and updated Prompt Forge's approved-models list accordingly." }
    ]
  },
  {
    "version": "1.8.0",
    "date": "2026-04-14",
    "changes": [
      { "type": "feature", "description": "Weekly digest emails are now interactive: every company name, role theme, and stat number links through to a matching pre-filtered view in the app, with a context banner on /jobs that explains which digest slice you are viewing." },
      { "type": "improvement", "description": "Deep links from email now preserve the destination through login, so clicking a company or theme while signed out lands on the intended page after Google sign-in." },
      { "type": "improvement", "description": "The in-app digest viewer at /digests/[id] mirrors the email's link affordances one-to-one, so the same theme, company, and stat tokens are clickable in both surfaces." }
    ]
  },
  {
    "version": "1.7.3",
    "date": "2026-04-12",
    "changes": [
      { "type": "improvement", "description": "Switched company news fetching and chat from Gemini Pro to Flash, significantly reducing daily AI costs while maintaining quality." },
      { "type": "improvement", "description": "Tightened maxOutputTokens across all Gemini calls and reduced job description sizes sent for extraction to cut per-job token costs." }
    ]
  },
  {
    "version": "1.7.2",
    "date": "2026-03-28",
    "changes": [
      { "type": "fix", "description": "Weekly digest sends emails individually instead of batch so one Resend rejection (e.g. test-sender restriction) does not block all other recipients; checks SDK error responses and logs from/to before each send." },
      { "type": "improvement", "description": "Supabase auth and REST calls from the server and proxy now retry on transient network errors (e.g. connection reset) to reduce random 401s and AuthRetryableFetchError noise on flaky networks." },
      { "type": "improvement", "description": "Unified neutral editorial voice across AI copy: shared docs/voice.md, runtime voice injection, heuristic voice checks, and Prompt Forge Voice compliance for weekly digest." },
      { "type": "improvement", "description": "Job-level and company-level strategic insights now use plain analytical headlines and summaries aligned with the weekly digest tone." }
    ]
  },
  {
    "version": "1.7.1",
    "date": "2026-03-20",
    "changes": [
      { "type": "fix", "description": "Admin dashboard stats now load correctly after migrating from the removed cron_logs table to job_runs." },
      { "type": "fix", "description": "Cron endpoints now require a valid bearer token; User-Agent alone no longer grants access." },
      { "type": "fix", "description": "Job collection and report triggers from the admin panel are now visible in the job log." }
    ]
  },
  {
    "version": "1.7.0",
    "date": "2026-03-18",
    "changes": [
      { "type": "feature", "description": "Competitive matrix now supports historical comparison across 30/60/90/180/360 day windows with an option to include closed roles." },
      { "type": "fix", "description": "All active tracked companies now always appear in the competitive matrix, even when they have zero current job postings." },
      { "type": "fix", "description": "Dashboard weekly net-job metrics now use a consistent rolling 7-day window for closed-job counts, including the competitive matrix delta." },
      { "type": "fix", "description": "Weekly digest company highlights and global summary no longer truncate mid-sentence; full AI-generated text is preserved." }
    ]
  },
  {
    "version": "1.6.0",
    "date": "2026-03-06",
    "changes": [
      { "type": "feature", "description": "Prompt Forge adds an admin-only Labs workflow for tuning AI prompts, comparing Gemini models, and shipping winning configs live." },
      { "type": "feature", "description": "Admins can now save active AI prompt/model settings, replay evaluation runs, and promote approved prompt changes into GitHub implementation specs." },
      { "type": "improvement", "description": "Tech stack extraction and synthesis now read validated runtime prompt settings so improved prompts apply immediately to new runs and backfills." },
      { "type": "improvement", "description": "Stage 1 reprocessing is more resilient with better truncated-response recovery, tighter output budgeting, and clearer operational guidance." },
      { "type": "improvement", "description": "Company insights and tech stack synthesis now preserve more named systems while filtering ambiguous or low-signal research sources more aggressively." },
      { "type": "feature", "description": "Admins can now refresh strategic insights for all companies from the dashboard, and company insight generation degrades safely when lock infrastructure is unavailable." }
    ]
  },
  {
    "version": "1.5.0",
    "date": "2026-03-06",
    "changes": [
      { "type": "feature", "description": "Company Overview now shows the full strategic analysis inline — no separate insight detail page needed" },
      { "type": "improvement", "description": "Tech stack section integrated natively into the analysis flow on the company Overview tab" },
      { "type": "improvement", "description": "Generate Insight button moved to the company header for one-click access from the Overview" }
    ]
  },
  {
    "version": "1.4.0",
    "date": "2026-03-06",
    "changes": [
      { "type": "feature", "description": "Tech stack analysis now auto-generates after job collection with AI-enriched summaries" },
      { "type": "improvement", "description": "Redesigned company page with cleaner layout, streamlined navigation, and consistent styling" },
      { "type": "fix", "description": "Fixed tech stack generation failing silently due to invalid AI model configuration" }
    ]
  },
  {
    "version": "1.3.0",
    "date": "2026-03-05",
    "changes": [
      { "type": "feature", "description": "Horizontal Gantt-style timeline showing initiative timeframes at a glance" },
      { "type": "improvement", "description": "Summary stats row with roles mapped, ongoing count, top category, and confidence distribution" },
      { "type": "improvement", "description": "Multi-phase loading with progress bar replaces plain spinner during analysis" },
      { "type": "improvement", "description": "Structured AI assessment with bold headline and key risk callout" },
      { "type": "feature", "description": "Category filter bar to show/hide initiative cards by type" },
      { "type": "improvement", "description": "Role tags now link to job detail pages and show relevance" },
      { "type": "feature", "description": "Copy Analysis button exports results as formatted Markdown" },
      { "type": "improvement", "description": "Multi-expand initiative cards allow side-by-side comparison" }
    ]
  },
  {
    "version": "1.2.0",
    "date": "2026-02-23",
    "changes": [
      { "type": "improvement", "description": "Hot Roles on the dashboard now link directly to the job details page instead of the company overview" },
      { "type": "feature", "description": "Feedback-to-code pipeline: accepted feedback automatically creates GitHub Issues with AI-generated specs" },
      { "type": "feature", "description": "Generate Code button in admin panel triggers Claude Opus 4.6 to implement issues on a feature branch and open a draft PR" },
      { "type": "feature", "description": "Admin email notifications when new feedback is submitted" },
      { "type": "feature", "description": "GitHub Issue status comments track implementation progress with links to workflow runs and draft PRs" },
      { "type": "improvement", "description": "Add Company page now routes through the feedback dialog for agent-driven setup" },
      { "type": "fix", "description": "Admin panel shows actions for all feedback states including accepted items missing a GitHub Issue" }
    ]
  },
  {
    "version": "1.1.1",
    "date": "2026-02-21",
    "changes": [
      { "type": "improvement", "description": "Upgraded strategic analysis and insight chat to Gemini 3.1 Pro for deeper reasoning and more accurate web grounding" }
    ]
  },
  {
    "version": "1.1.0",
    "date": "2026-02-17",
    "changes": [
      { "type": "feature", "description": "Submit feedback directly from the app with AI-powered triage" },
      { "type": "improvement", "description": "Contextual back navigation on job detail pages" }
    ]
  },
  {
    "version": "1.0.0",
    "date": "2026-02-16",
    "changes": [
      { "type": "feature", "description": "Release history page and newsletter footer" },
      { "type": "feature", "description": "Rebrand to The Fintech Talent Brief" },
      { "type": "feature", "description": "Stratechery-style analyst briefing format for weekly digests" },
      { "type": "feature", "description": "Intelligence-first dashboard redesign" }
    ]
  }
]
